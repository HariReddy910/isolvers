this was an issue from before and it was fixed in ur deploy script
why is it back again? 
we had the same issue in NSS_ATI_MG deploy. It deals with the same instance. The script was modified for 1502 server
can u pls check the NSS_ATI_MG deploy script and make the same changes for NSS_DDGEN_MG for the 1502 server?
Also looks like the script is bringing up DR instances.. It needs to be down.
While this issue is sorted - can u pls begin the deploy in CERT?

L3PVAP1156
L3PVAP1155
L1BVAP1030
L1BVAP1031





list="L1BVAP1030 L1BVAP1031"
for x in $list
do
echo "SSH connectivity check for $x"
scp acm/acm_test/testFile webdep@${x:?}:/tmp
echo
echo "Space availability for $x"
ssh ${x:?} df -h /opt/escm || ssh ${x:?} df -h /home/webdep || ssh ${x:?} df -h /export/home/webdep
echo
done

fdcproxy.1fdc.com
port 8080




list="r1pvap1331 r1pvap1332"
for x in $list
do
echo "SSH connectivity check for $x"
scp acm/acm_test/sample webdep@${x:?}:/tmp
echo
echo "Space availability for $x"
ssh ${x:?} df -h /opt/escm || ssh ${x:?} df -h /home/webdep || ssh ${x:?} df -h /export/home/webdep
echo
done

CRQ000000179053

------------- websphere ---------------------


webdep
Welcome1
welcome1
Welcome01
welcome01

webdepmiddleware 
6J5pAVpc9

--------------------------------------------------------------

0001- onsite num-851064#

-------------------------------------------------------------

•         Identify the old File System [ cd /export/home or cd/home/webdep ]
•         Copy the .ssh file from /export/home or /home/webdep to /opt/escm [ cp –R <oldFileSystem>/.ssh  /opt/escm/ ]

Please follow the detailed steps from the mail chain if necessary.

First check SSh connectivity in agent, if it is working fine leave it.

If its not working then loginto target server and switch to webdep

Su – webdep
Pwd

If it is /opt/escm  check ls –la

It wont show .ssh file  then got  /export/home/webdep

Give ls –la

It will display .ssh file

Give copy command 

Cp –R .ssh/  /opt/escm

Then got /opt/escm

Cd /opt/escm

Ls -la




-------------------------


product_Name ACM --> project Bitbucket 
component_Name in ACM --> repositories in Bitbucket


--------------------------------------

git clone --mirror <gerrit repo url>
cd <repoName>
git push --mirror <stash repo url>

give repo access to user & initiate mail
go to project edit in ACM
change PROJECT_BUILD_EXECUTE_PATH & PROJECT_BUILD_OUTPUT_PATH to lower case

 Go to gerrit server to inactivate project
 
 Login gerrit server – n3pvap1003
 dzdo su – webdep
 cd /opt/escm/repos
 ls –ltr <applicationName>
 mv <applicationName> InactiveProjects/
 
 ls  CONNECT-PAY_APP.git
 mv CONNECT-PAY_APP.git InactiveProjects/
 
 
 
 
 https://escmjenkins.1dc.com/job/fs-rewards-ui-services-build-fortify/configure
 
 clean install -P fortify-sca -DskipTests=true
 
 cd /opt/escm/acm/tool/webinspect/
webinspect.sh "UAID-06033" fs-rewards-ui-services Mohaseen.bagwan@firstdata.com "https://l1qvap1128.1dc.com:12074/api/fs/rewardsadmin/v2/"
webinspect.sh "UAID-06033" fs-rewards-ui-services Mohaseen.bagwan@firstdata.com "https://l1qvap1127.1dc.com:12057/api/fs/rewardsui/v2/"


















































